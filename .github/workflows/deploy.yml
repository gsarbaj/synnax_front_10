name: Deploy synnax.lt

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  NODE_VERSION: '20.x'
  WORKDIR: .
  APP_NAME: app_synnax
  RELEASES_DIR: /var/www/synnax/releases
  CURRENT_LINK: /var/www/synnax/current
  SSH_PORT: ${{ vars.SSH_PORT || 22 }}

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      # (разово) проверим, что lock-файл в корне
      - name: 🔎 Debug paths
        run: |
          ls -la
          [ -f "package-lock.json" ] && echo "package-lock.json FOUND" || (echo "package-lock.json MISSING" && exit 1)

      - name: 🟢 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      # Ручной кэш npm по хэшу корневого package-lock.json
      - name: ♻️ Cache npm (~/.npm)
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: npm-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            npm-

      - name: 📦 Install deps (npm ci)
        env:
          npm_config_cache: ~/.npm
        run: npm ci

      - name: 🔐 Create .env.production
        run: printf "%s" "${{ secrets.ENV_FILE_PRODUCTION }}" > .env.production

      - name: 🏗️ Build (Next.js standalone)
        env:
          NODE_ENV: production
          NEXT_TELEMETRY_DISABLED: '1'
        run: npm run build

      - name: 📁 Pack deploy bundle
        run: |
          set -euxo pipefail
          mkdir -p deploy_bundle
          rsync -a --delete ".next/standalone/" deploy_bundle/
          rsync -a ".next/static" deploy_bundle/.next/static
          rsync -a "public" deploy_bundle/public || true
          cp ".env.production" deploy_bundle/.env.production
          echo "Build $GITHUB_REPOSITORY@$GITHUB_SHA" > deploy_bundle/RELEASE.txt

      # ==== SSH auth через ssh-agent (надежно) ====
      - name: 🔑 Start ssh-agent (load SSH_PRIVATE_KEY)
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: |
            ${{ secrets.SSH_PRIVATE_KEY }}

      - name: 🧾 Add known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p ${{ env.SSH_PORT }} -H "${{ secrets.SSH_HOST }}" >> ~/.ssh/known_hosts
          chmod 644 ~/.ssh/known_hosts

      - name: 🔍 Debug ssh-agent keys
        run: ssh-add -l || true

      - name: 🔌 Test SSH connectivity
        run: |
          ssh -o StrictHostKeyChecking=yes -p ${{ env.SSH_PORT }} \
            "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}" \
            'whoami && echo OK'

      # ==== Деплой ====
      # === Upload bundle ===
      - name: 📤 Upload bundle to server (new release)
        env:
          TS: ${{ github.run_id }}-${{ github.run_attempt }}
        run: |
          set -euxo pipefail
          RELEASE_DIR="${RELEASES_DIR}/${TS}"
          # создаём каталог релиза на сервере
          ssh -p "${SSH_PORT}" "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}" "mkdir -p '${RELEASE_DIR}'"
          # выгружаем бандл
          rsync -e "ssh -p ${SSH_PORT}" -az --delete \
            deploy_bundle/ \
            "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${RELEASE_DIR}/"

      # === Switch + restart ===
      - name: 🔁 Atomic symlink switch + tmux restart
        env:
          TS: ${{ github.run_id }}-${{ github.run_attempt }}
        run: |
          set -euxo pipefail
          RELEASE_DIR="${RELEASES_DIR}/${TS}"

          # передаём переменные окружения в удалённую сессию и запускаем скрипт
          ssh -p "${SSH_PORT}" "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}" \
            CURRENT_LINK="${CURRENT_LINK}" \
            APP_NAME="${APP_NAME}" \
            RELEASE_DIR="${RELEASE_DIR}" \
            'bash -s' <<'EOSSH'
set -euo pipefail

# 1) sanity checks
[ -d "$RELEASE_DIR" ] || { echo "Release dir missing: $RELEASE_DIR"; exit 1; }
mkdir -p "$(dirname "$CURRENT_LINK")"

# 2) переключаем current → новый релиз (атомарно)
ln -sfn "$RELEASE_DIR" "$CURRENT_LINK"

# 3) перезапуск через tmux (молча, если сессии нет)
if command -v tmux >/dev/null 2>&1; then
  tmux kill-session -t "$APP_NAME" >/dev/null 2>&1 || true
else
  echo "tmux not found on server"; exit 1
fi

cd "$CURRENT_LINK"

# 4) подгружаем env, если есть
set -a; [ -f ./.env.production ] && . ./.env.production; set +a

: "${PORT:=3000}"
: "${HOST:=127.0.0.1}"

tmux new -d -s "$APP_NAME" "cd '$CURRENT_LINK' && NODE_ENV=production HOST=$HOST PORT=$PORT node server.js"

# 5) лёгкая проверка
tmux ls >/dev/null 2>&1 && echo "tmux session '$APP_NAME' started"
EOSSH
      
      - name: ✅ Smoke test
        if: ${{ vars.PUBLIC_URL != '' }}
        run: curl -fsSL --max-time 5 "${{ vars.PUBLIC_URL }}/" >/dev/null